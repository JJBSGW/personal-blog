# 个人博客项目计划

> 状态:进行中。阶段 1 已完成(✅),当前推进到阶段 2。每完成一个 Step 即提交并推送到 GitHub。

## 一、目标
做一个**全栈动态个人博客**,部署到自己的云服务器,公网可访问。
功能:文章发布与管理、全文搜索、暗色模式、阅读统计,并预留扩展空间(评论、特效组件等)。

## 二、技术栈(最终版)
| 层 | 选型 | 理由 |
|---|---|---|
| 框架 | Next.js (App Router) + TypeScript + Tailwind CSS | React 生态组件/特效最丰富;前后端一体,一个项目搞定 |
| 数据库 | PostgreSQL 18 + Prisma 7 (ORM) | 稳定强大,便于扩展 |
| 认证 | NextAuth (Auth.js) | 后台登录,仅自己可管理 |
| 搜索 | Meilisearch | 中文分词效果好,自托管免费 |
| 暗色模式 | next-themes | 明暗切换 + 记住偏好 |
| 部署 | Docker Compose + Caddy | 一条命令起全部服务,Caddy 自动 HTTPS |

## 三、写作方式(不需要懂网页代码)
1. 浏览器打开博客 `/admin` 登录
2. 在 Markdown 编辑器里写文章(带实时预览)
3. 点发布,前台立即可见
> Markdown 语法(标题/加粗/链接/代码块等)5 分钟可学会,和写微信文章排版类似。

## 四、功能范围
**核心(本期全做)**
- 前台:首页文章列表、文章详情(代码高亮 + 目录 TOC)、标签/分类页、关于页、**个人简历页**、RSS
- 后台:登录认证、Markdown 编辑器写文章、文章管理(编辑/删除/状态)
- 全文搜索:搜索框 + 结果页(中文友好)
- 暗色模式:明/暗切换,跟随系统
- 阅读统计:浏览量计数(防刷),后台趋势图表
- **个人简历页**(`/resume`):结构化展示教育背景、研究/项目经历、专业技能、获奖荣誉等,支持一键打印 / 导出 PDF,方便 HR 与网友了解你

**预留扩展(架构上留好位置,以后按需加)**
- 评论、草稿、定时发布、友链
- 视觉效果:shadcn/ui 组件、framer-motion 动画、three.js 3D 特效

## 五、数据模型
- `User` — 管理员账号
- `Post` — 标题、内容(Markdown)、slug、摘要、状态、发布时间
- `Tag` / `Category` — 标签与分类(多对多)
- `View` — 阅读记录(文章、日期、IP),用于统计
- `Resume` — 简历结构化数据(教育/项目/技能/经历等板块,可在后台维护)
- (预留)`Comment` — 评论

## 六、实施阶段(业余时间约 3~5 周)
| 阶段 | 内容 | 预估 |
|---|---|---|
| 1. 初始化 | Next.js + TS + Tailwind + Prisma 脚手架,数据库连通,目录结构 | 2~3 天 |
| 2. 前台基础页 | 列表页、详情页、标签页、关于页、**简历页(打印/导出 PDF)**、RSS | 4~5 天 |
| 3. 后台管理 | 登录、Markdown 编辑器、文章管理 | 3~4 天 |
| 4. 全文搜索 | 接入 Meilisearch,搜索页 | 2~3 天 |
| 5. 暗色 + 统计 | 主题切换、浏览量、后台趋势图表 | 2 天 |
| 6. 部署上线 | 购买服务器 → Docker Compose → Caddy HTTPS → 域名 → 公网访问 | 2~3 天 + 备案等待(若国内节点) |
| 7. 持续优化 | SEO、性能、备份策略,开始写内容 | 长期 |

## 七、服务器与域名建议
- **服务器规格**:2 核 CPU / 2~4 GB 内存 / 40~80 GB SSD,系统 Ubuntu 22.04 LTS
  - 2 GB 内存够用;4 GB 更宽裕(Meilisearch 稍吃内存)
- **国内节点**(阿里云/腾讯云轻量服务器):访问快,但域名需 **ICP 备案**(约 1~2 周),学生优惠约 100~300 元/年
- **香港/海外节点**(腾讯云轻量香港 / RackNerd / Vultr):**免备案**,开箱即用,国内访问稍慢,约 $5/月 或年付更省
- **域名**:.com 约 50~80 元/年,注册需实名
- 预算参考:年付 200~400 元足够

## 八、部署拓扑
```
公网 → Caddy (443 HTTPS, 自动证书)
         ├─ next-app (:3000)
         ├─ postgres (:5432, 仅内网)
         └─ meilisearch (:7700, 仅内网)
```
Docker Compose 编排,`docker compose up -d` 一键启动。

## 九、关键注意点
- 中文搜索靠 Meilisearch 分词,体验优于数据库自带搜索
- 服务器只开放 80/443,SSH 密钥登录,数据库/搜索不对外网
- 阅读统计按 IP + 时间窗口去重防刷
- 数据库定时备份,Git 备份代码

## 十、安全与运行约定(已确立)
1. **一切都在工作区内**:代码、依赖(`node_modules`)、缓存(`.cache/`)、环境变量(`.env`)全部位于 `F:\MasterWork\PersonalBlog`,不写系统目录
2. **运行时用 conda 环境**:`conda activate blog`(nodejs 22 + python 3.12,建在用户自己的 Anaconda 下);没有 conda 时任意 Node ≥ 20.9 亦可,项目不依赖 Python
3. **系统零修改**:不安装 Docker/PostgreSQL 等系统级服务;本地数据库是嵌入式 PostgreSQL 18(真实二进制,数据在 `.cache/pgdata`)
4. **版本管理**:git + GitHub 仓库 `JJBSGW/personal-blog`,每个 Step 完成即提交推送
5. **沙箱说明**:开发服务器 / 数据库 / prisma 命令需临时进程权限,是会话环境限制;用户在自己终端执行**不需要**任何特殊权限

## 十一、Step 推进计划(每步 = 目标 → 动作 → 验证 → 提交)

> 约定:每完成一个 Step 执行一次 `git commit + push`,并可由你在 localhost 预览。

### 阶段 2:前台页面(当前)
| Step | 内容 | 验证标准 |
|---|---|---|
| **S2.1** | 布局骨架:根布局、导航栏(首页/标签/关于/简历)、页脚、全局样式、中文字体、基础明暗变量 | 首页显示博客外壳,导航可点击跳转 |
| **S2.2** | 首页文章列表:分页、摘要、标签、按发布时间倒序(仅显示已发布) | 列表正确展示种子文章 |
| **S2.3** | 文章详情页 `/posts/[slug]`:Markdown 渲染、代码高亮、目录 TOC、上一篇/下一篇 | 打开文章,代码高亮与目录正常 |
| **S2.4** | 标签页 `/tags` 与分类页:按标签/分类过滤文章 | 过滤结果正确 |
| **S2.5** | 关于页 `/about`:站点介绍(内容后台可维护) | 页面展示正常 |
| **S2.6** | 简历页 `/resume`:结构化展示 + 打印样式 + 导出 PDF | 浏览器打印预览不含导航栏,可导出 PDF |
| **S2.7** | RSS 订阅 `/feed.xml`:动态生成 | XML 可被订阅器解析 |
| **里程碑** | 阶段 2 完成 → 提交 "阶段2 前台页面" | 全站可浏览 |

### 阶段 3:后台管理
| Step | 内容 | 验证标准 |
|---|---|---|
| **S3.1** | 认证:NextAuth 邮箱+密码登录(单管理员,密码哈希存储) | 未登录访问 /admin 自动跳登录页 |
| **S3.2** | 后台框架:布局、侧边栏、路由保护 | 登录后进入后台,未登录被拦截 |
| **S3.3** | Markdown 编辑器:编辑区 + 实时预览分屏 | 输入即预览 |
| **S3.4** | 文章管理:新建/编辑/删除、草稿/发布、slug 自动生成、标签与分类管理 | 完整增删改查流程可用 |
| **S3.5** | 简历/关于维护:后台编辑结构化简历数据与关于页内容 | 保存后前台即时更新 |
| **里程碑** | 阶段 3 完成 → 提交 "阶段3 后台管理" | 后台全流程可用 |

### 阶段 4:全文搜索
| Step | 内容 | 验证标准 |
|---|---|---|
| **S4.1** | Meilisearch 服务:本地二进制放入 `.cache/`(与嵌入式 PG 同思路),生产走 Docker | 服务健康检查通过 |
| **S4.2** | 索引同步:文章发布/更新/删除时增量同步到 Meilisearch | 数据与数据库一致 |
| **S4.3** | 搜索页 `/search`:输入即搜、关键词高亮、空状态 | 中文分词搜索命中准确 |
| **里程碑** | 阶段 4 完成 → 提交 "阶段4 全文搜索" | 站内搜索可用 |

### 阶段 5:暗色模式 + 阅读统计
| Step | 内容 | 验证标准 |
|---|---|---|
| **S5.1** | next-themes 明暗切换:记忆偏好、跟随系统 | 切换即时生效并记住 |
| **S5.2** | 浏览量计数:接口按 IP + 时间窗口去重,更新 `viewCount` | 刷新不重复计数 |
| **S5.3** | 后台统计:近 30 天阅读趋势图 | 图表随数据更新 |
| **里程碑** | 阶段 5 完成 → 提交 "阶段5 暗色与统计" | 特性齐备 |

### 阶段 6:部署上线
| Step | 内容 | 验证标准 |
|---|---|---|
| **S6.1** | 服务器准备:购买(香港节点免备案优先)、Ubuntu 22.04、SSH 密钥登录、防火墙仅开 80/443 | 能 SSH 登录,端口收敛 |
| **S6.2** | Docker Compose 编排:next-app + postgres + meilisearch + caddy,环境变量与健康检查 | `docker compose up -d` 一键起 |
| **S6.3** | 域名 + HTTPS:Caddy 自动申请证书,绑定域名 | 公网 https 访问成功 |
| **S6.4** | CI/CD(可选):GitHub Actions 推送后自动构建部署 | 提交代码自动上线 |
| **里程碑** | 阶段 6 完成 → 提交 "阶段6 部署上线" | 公网可访问 |

### 阶段 7:持续迭代
| Step | 内容 |
|---|---|
| **S7.1** | SEO:metadata、sitemap.xml、Open Graph 分享图 |
| **S7.2** | 性能优化:图片优化、缓存策略、Core Web Vitals |
| **S7.3** | 备份:定时 `pg_dump` 数据库备份,Git 即代码备份 |
| **S7.4** | 内容:开始写作,按需加扩展功能(评论、特效组件等) |
